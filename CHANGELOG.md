# Changelog

## Unreleased

- First skeleton: `vectors` runner with per-vector reporting, `request` /
  `response` inspection with signature-base reconstruction and optional
  verification, `card` proof verification; JSON output.
